# Where the hosted Gwanggo API lives. The studio sends all generation
# requests here. Default points at the official hosted API.
NEXT_PUBLIC_API_URL=https://api.gwanggo.ai

# Where users go to create/manage API keys (the hosted dashboard).
NEXT_PUBLIC_DASHBOARD_URL=https://gwanggo.ai/dashboard/api-keys
